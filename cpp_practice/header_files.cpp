#include "add.h"
#include <iostream>
// header files only contain the forward declarations of functions typically
// header file should have the ame name as the .cpp file that the functon definition exists in 

int main(void) {
    std::cout << adder(1,6) << std::endl;

}